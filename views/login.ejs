<!DOCTYPE html>
<html lang="<%= t.lang %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= t.login_title %> — ProjectHub</title>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body class="login-body">
  <div class="login-container">
    <div class="login-card">
      <!-- Language Toggle -->
      <div class="lang-toggle-login">
        <a href="/lang/fr" class="lang-btn <%= t.lang === 'fr' ? 'active' : '' %>">🇫🇷 FR</a>
        <a href="/lang/en" class="lang-btn <%= t.lang === 'en' ? 'active' : '' %>">🇬🇧 EN</a>
      </div>

      <div class="login-header">
        <div class="logo-icon logo-icon-lg">P</div>
        <h1>ProjectHub</h1>
        <p><%= t.login_subtitle %></p>
      </div>

      <% if (typeof error !== 'undefined' && error) { %>
        <div class="alert alert-error">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="15" y1="9" x2="9" y2="15"/><line x1="9" y1="9" x2="15" y2="15"/></svg>
          <%= error %>
        </div>
      <% } %>

      <form method="POST" action="/login" class="login-form">
        <div class="form-group">
          <label for="email"><%= t.login_email || 'Email' %></label>
          <input type="email" id="email" name="email" required autofocus placeholder="<%= t.login_email_placeholder || 'votre@email.com' %>">
        </div>
        <div class="form-group">
          <label for="password"><%= t.login_password %></label>
          <input type="password" id="password" name="password" required placeholder="<%= t.login_password_placeholder %>">
        </div>
        <button type="submit" class="btn btn-primary btn-full">
          <%= t.login_submit %>
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
        </button>
      </form>

      <div class="register-footer" style="text-align:center;margin-top:1.5rem;padding-top:1.5rem;border-top:1px solid var(--border);">
        <p style="color:var(--text-muted);font-size:.9rem;">
          <%= t.login_no_account || 'Pas encore de compte ?' %>
          <a href="/register" style="color:var(--accent);font-weight:600;text-decoration:none;">
            <%= t.login_register_link || 'Créer un compte gratuit' %>
          </a>
        </p>
      </div>
    </div>
  </div>
</body>
</html>
