<!DOCTYPE html>
<html lang="<%= t.lang %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= t.help_search %> — ProjectHub</title>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/help.css">
</head>
<body class="help-body">
  <nav class="help-nav">
    <div class="help-nav-inner">
      <a href="/help" class="help-logo">
        <div class="logo-icon">P</div>
        <span><%= t.help_title %></span>
      </a>
      <div class="help-nav-actions">
        <div class="lang-toggle">
          <a href="/lang/fr" class="lang-btn <%= t.lang === 'fr' ? 'active' : '' %>">FR</a>
          <a href="/lang/en" class="lang-btn <%= t.lang === 'en' ? 'active' : '' %>">EN</a>
        </div>
        <% if (user) { %>
          <a href="/" class="btn btn-ghost btn-sm"><%= t.help_back_app %></a>
        <% } else { %>
          <a href="/login" class="btn btn-ghost btn-sm"><%= t.login_submit %></a>
        <% } %>
      </div>
    </div>
  </nav>

  <div class="help-search-header">
    <form action="/help/search" method="GET" class="help-search-form help-search-compact">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
      <input type="text" name="q" value="<%= query %>" placeholder="<%= t.help_search_placeholder %>" autofocus>
      <button type="submit" class="btn btn-primary btn-sm"><%= t.help_search_btn %></button>
    </form>
  </div>

  <main class="help-main">
    <div class="help-search-results">
      <h2><%= t.help_search_results %> "<%= query %>" <span class="result-count">(<%= articles.length %>)</span></h2>

      <% if (articles.length > 0) { %>
        <div class="help-results-list">
          <% articles.forEach(article => { %>
            <a href="/help/article/<%= article.slug %>" class="help-result-item">
              <div class="help-result-meta">
                <span class="help-article-cat"><%= article.displayCategory %></span>
                <% if (!article.is_public) { %><span class="help-private-badge">🔒 <%= t.help_staff_only %></span><% } %>
              </div>
              <h3><%= article.displayTitle %></h3>
              <p><%= article.displayExcerpt || '' %></p>
            </a>
          <% }) %>
        </div>
      <% } else { %>
        <div class="help-empty">
          <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" opacity="0.4"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/></svg>
          <h3><%= t.help_no_results %></h3>
          <p><%= t.help_no_results_text %></p>
          <a href="/help" class="btn btn-ghost"><%= t.help_back_home %></a>
        </div>
      <% } %>
    </div>
  </main>

  <footer class="help-footer">
    <p>© <%= new Date().getFullYear() %> ProjectHub — <%= t.help_footer %></p>
  </footer>
</body>
</html>
